{
    "entryPoints": ["src/**/*.ts"],          // The files or directories to include
    "exclude": ["**/*.spec.ts"],         // Exclude test files
    "tsconfig": "tsconfig.json",         // Use the project's TypeScript configuration
    "out": "docs",                       // Directory for generated documentation
    "includeVersion": true,              // Optionally include the project's version
    "hideGenerator": true,               // Hide the "Generated by TypeDoc" footer
    "theme": "default"                   // The theme to use for the output
    }